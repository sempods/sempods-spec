name: Goal or multi-step plan
description: Plan a larger specification goal, substantial proposal or repository improvement.
body:
  - type: markdown
    attributes:
      value: |
        Read [GOVERNANCE.md](https://github.com/sempods/sempods-spec/blob/main/GOVERNANCE.md)
        and [Issue planning](https://github.com/sempods/sempods-spec/blob/main/docs/agents/documentation-strategy.md#issue-planning).
        Search existing issues before filing. Describe the intended iterations and link related issues.
        Maintainers or triagers set native relationships, suitable labels and any agreed release milestone.
        You do not need those permissions to file this form. Security reports go privately via the [security policy](https://github.com/sempods/.github/blob/main/SECURITY.md).
  - type: textarea
    id: problem
    attributes:
      label: Problem
      description: What needs to change, and why?
    validations:
      required: true
  - type: textarea
    id: target
    attributes:
      label: Target and scope
      description: Describe the intended result and boundaries. Link substantive design if needed.
    validations:
      required: true
  - type: textarea
    id: acceptance
    attributes:
      label: Acceptance
      description: Observable conditions for completing the whole goal, beyond finishing its children.
    validations:
      required: true
  - type: textarea
    id: dependencies
    attributes:
      label: Dependencies and decisions
      description: Link prerequisite issues and identify unresolved decisions. Suggest release scope or labels if useful; maintainers or triagers set the metadata.
  - type: markdown
    attributes:
      value: |
        Each PR runs documentation-sync and updates affected documentation in that PR, or records why no update is needed.
        Before closure, verify all acceptance and record merged PRs, check results and documentation evidence.
  - type: textarea
    id: documentation
    attributes:
      label: Documentation completion
      description: Name likely documentation owners and planned updates, or explain why none are affected.
    validations:
      required: true
